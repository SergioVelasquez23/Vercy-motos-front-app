# Configuración de Variables de Entorno - Vercy Motos
# =====================================================

# AMBIENTE DE EJECUCIÓN
# Valores posibles: development, staging, production
FLUTTER_ENV=development

# CONFIGURACIÓN DEL SERVIDOR BACKEND
# ==================================

# URL base del servidor API (opcional - se auto-detecta si no se especifica)
# API_BASE_URL=http://192.168.1.100:8081

# URL alternativa del backend (fallback)
# BACKEND_URL=http://localhost:8081

# Configuraciones específicas por ambiente
# ========================================

# DESARROLLO (development)
# - Detección automática: HABILITADA
# - Logging: HABILITADO
# - Puerto por defecto: 8081

# STAGING (staging) 
# - Detección automática: HABILITADA
# - Logging: HABILITADO
# - Puerto por defecto: 8081

# PRODUCCIÓN (production)
# - Detección automática: DESHABILITADA
# - Logging: DESHABILITADO  
# - Puerto por defecto: 8080
# - URL fallback: http://api.sopacarbon.com:8080

# CONFIGURACIONES ADICIONALES
# ===========================

# Tiempo de cache para detección de IP (en minutos)
# NETWORK_CACHE_MINUTES=5

# Timeout para conexiones HTTP (en segundos)
# HTTP_TIMEOUT=15

# Modo debug para red (true/false)
# DEBUG_NETWORK=false

# =====================================================
# INSTRUCCIONES DE USO:
# =====================================================
#
# 1. Copia este archivo como .env en la raíz del proyecto:
#    cp .env.example .env
#
# 2. Ajusta las variables según tu entorno:
#    - Para desarrollo local: usar variables por defecto
#    - Para testing: especificar API_BASE_URL si es necesario
#    - Para producción: SIEMPRE especificar API_BASE_URL
#
# 3. El archivo .env NO debe ser committeado al repositorio
#    (ya está en .gitignore)
#
# 4. Para usar en Flutter, las variables deben pasarse en tiempo de compilación:
#    flutter run --dart-define=FLUTTER_ENV=development
#    flutter run --dart-define=API_BASE_URL=http://192.168.1.50:8081
#
# 5. Para Android/iOS release builds:
#    flutter build apk --dart-define=FLUTTER_ENV=production --dart-define=API_BASE_URL=http://production-server:8080
#
# =====================================================
